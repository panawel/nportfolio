# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# raw, uncompressed originals kept locally — never shipped or committed
/assets/originals

# old local design notes (predate the current light theme), not part of the published repo
/design-system

# local Claude Code tooling (third-party design-intelligence skill data, not part of the shipped site)
/.claude/skills

# typescript
*.tsbuildinfo
next-env.d.ts
